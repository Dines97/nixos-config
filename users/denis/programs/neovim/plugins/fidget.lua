require('fidget').setup({
  progress = {
    display = {
      done_ttl = 5,
      progress_icon = {
        pattern = 'dots'
      }
    }
  }
  -- notification = {
  --   override_vim_notify = true
  --   -- window = {
  --   --   align = 'top'
  --   -- }
  -- }
})
